<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itxindeshang.mapper.CategoryMapper">
    <select id="countByIds" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM category WHERE id IN
        <foreach collection="categoryIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    <select id="selectApplicableCategoryIds" resultType="java.lang.Long">
        SELECT DISTINCT COALESCE(child.id, cc.category_id)
        FROM coupon_category cc
        JOIN category parent ON cc.category_id = parent.id
        LEFT JOIN category child
            ON child.parent_id = cc.category_id
            AND parent.parent_id = 0
        WHERE cc.coupon_id = #{couponId}
    </select>
</mapper>